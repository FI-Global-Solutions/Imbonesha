"""Imagery app — AOI boundaries and image scene catalogue."""

from django.apps import AppConfig


class ImageryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "imagery"
